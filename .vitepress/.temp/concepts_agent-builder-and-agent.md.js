import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Agent Builder And Agent","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/agent-builder-and-agent.md","filePath":"concepts/agent-builder-and-agent.md","lastUpdated":null}');
const _sfc_main = { name: "concepts/agent-builder-and-agent.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="agent-builder-and-agent" tabindex="-1">Agent Builder And Agent <a class="header-anchor" href="#agent-builder-and-agent" aria-label="Permalink to &quot;Agent Builder And Agent&quot;">​</a></h1><p><code>AgentBuilder</code> is the setup object. It gathers the agent&#39;s provider, model, instructions, tools, middleware, stores, and other configuration.</p><p><code>Agent</code> is the runtime object returned by <code>BuildAsync()</code>. It accepts user input through <code>RunAsync(...)</code>, calls the configured chat client, coordinates tools and middleware, emits events, and returns an <code>AgentTurnResult</code>.</p><p>For the larger runtime map, including events, sessions, branches, tools, middleware, hosting, observability, evaluations, and trust boundaries, see <a href="./agent-runtime-and-capabilities">Agent Runtime And Capabilities</a>.</p><h2 id="the-first-reader-flow" tabindex="-1">The First-Reader Flow <a class="header-anchor" href="#the-first-reader-flow" aria-label="Permalink to &quot;The First-Reader Flow&quot;">​</a></h2><p>The basic flow is:</p><ol><li>Create a new <code>AgentBuilder</code>.</li><li>Add a provider, such as OpenAI or Ollama.</li><li>Add instructions.</li><li>Await <code>BuildAsync()</code>.</li><li>Await <code>RunAsync(...)</code>.</li><li>Read <code>AgentTurnResult.Text</code> for the final assistant text.</li></ol><p>Build-time configuration and run-time execution are separate. <code>BuildAsync()</code> creates a configured agent. <code>RunAsync(...)</code> performs a message turn.</p><h2 id="provider" tabindex="-1">Provider <a class="header-anchor" href="#provider" aria-label="Permalink to &quot;Provider&quot;">​</a></h2><p>A chat provider supplies the model client used for the run. In the first quickstart, <code>.WithOpenAI(model: &quot;gpt-5-mini&quot;)</code> configures the default chat model. The local alternative uses <code>.WithOllama(model: &quot;llama3.2&quot;)</code>.</p><p>Provider packages also handle provider registration and secret resolution. If a provider is missing or not registered, the run can fail with a provider-registration error. See <a href="./providers-clients-and-secrets">Providers, Clients, And Secrets</a> and <a href="./../reference/provider-keys-and-env-vars">Provider Keys And Env Vars</a>.</p><h2 id="instructions" tabindex="-1">Instructions <a class="header-anchor" href="#instructions" aria-label="Permalink to &quot;Instructions&quot;">​</a></h2><p>Instructions are the agent-level guidance sent with the run. For example, a first agent might be told to be concise and helpful. Keep starter instructions short so the behavior is easy to observe.</p><h2 id="run-result" tabindex="-1">Run Result <a class="header-anchor" href="#run-result" aria-label="Permalink to &quot;Run Result&quot;">​</a></h2><p><code>RunAsync(...)</code> returns an <code>AgentTurnResult</code>. For first-reader examples, the important property is <code>Text</code>, which contains the final concatenated assistant text.</p><p>When streaming events are subscribed, event handlers can print text while the run is still happening. The final <code>AgentTurnResult.Text</code> is still available after the run completes. See <a href="./../getting-started/streaming-events">Streaming Events</a> and <a href="./sessions-branches-and-events">Sessions, Branches, And Events</a>.</p><h2 id="tools-and-middleware" tabindex="-1">Tools And Middleware <a class="header-anchor" href="#tools-and-middleware" aria-label="Permalink to &quot;Tools And Middleware&quot;">​</a></h2><p>Tools let the model call C# methods. Add one local tool after the hello-agent path is working. See <a href="./../getting-started/add-a-tool">Add a Tool</a> and <a href="./tools-functions-and-harnesses">Tools, Functions, And Harnesses</a>.</p><p>Middleware belongs after the first run and first tool. It can participate in lifecycle hooks, wrapping, permissions, state, retries, and formatting. See <a href="./middleware-lifecycle">Middleware Lifecycle</a>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("concepts/agent-builder-and-agent.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const agentBuilderAndAgent = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  agentBuilderAndAgent as default
};
