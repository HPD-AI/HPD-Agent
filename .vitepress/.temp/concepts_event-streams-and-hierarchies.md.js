import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Event Streams And Hierarchies","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/event-streams-and-hierarchies.md","filePath":"concepts/event-streams-and-hierarchies.md","lastUpdated":null}');
const _sfc_main = { name: "concepts/event-streams-and-hierarchies.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="event-streams-and-hierarchies" tabindex="-1">Event Streams And Hierarchies <a class="header-anchor" href="#event-streams-and-hierarchies" aria-label="Permalink to &quot;Event Streams And Hierarchies&quot;">​</a></h1><p>HPD Agent emits events as a stream. A console can print that stream in arrival order, a chat UI can project it into messages, a TUI can project it into panels, and an orchestration view can project it into a tree of workflows, agents, tools, and middleware prompts.</p><p>The important model is:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>event stream: ordered runtime events</span></span>
<span class="line"><span>event projection: the view your app builds from those events</span></span>
<span class="line"><span>hierarchy: one useful projection, built from correlation fields</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>Do not assume every event is born as a perfect tree node. Some events carry full span data, some carry message or tool ids, some carry agent metadata, and some are live-only coordination events. Use the fields that match the view you are building.</p><h2 id="flat-stream-structured-view" tabindex="-1">Flat Stream, Structured View <a class="header-anchor" href="#flat-stream-structured-view" aria-label="Permalink to &quot;Flat Stream, Structured View&quot;">​</a></h2><p>A live stream might arrive like this:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>WorkflowStartedEvent</span></span>
<span class="line"><span>WorkflowAgentStartedEvent</span></span>
<span class="line"><span>MessageTurnStartedEvent</span></span>
<span class="line"><span>TextMessageStartEvent</span></span>
<span class="line"><span>TextDeltaEvent</span></span>
<span class="line"><span>ToolCallStartEvent</span></span>
<span class="line"><span>PermissionRequestEvent</span></span>
<span class="line"><span>PermissionResponseEvent</span></span>
<span class="line"><span>ToolCallResultEvent</span></span>
<span class="line"><span>ToolCallEndEvent</span></span>
<span class="line"><span>TextMessageEndEvent</span></span>
<span class="line"><span>WorkflowAgentCompletedEvent</span></span>
<span class="line"><span>WorkflowCompletedEvent</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br></div></div><p>A UI can project the same stream like this:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>DraftAndReview workflow</span></span>
<span class="line"><span>  Drafter node</span></span>
<span class="line"><span>    message turn</span></span>
<span class="line"><span>      assistant message</span></span>
<span class="line"><span>      tool call</span></span>
<span class="line"><span>        permission request</span></span>
<span class="line"><span>        result</span></span>
<span class="line"><span>  workflow complete</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>That projection is a client responsibility. HPD gives you the event families and correlation fields; your app decides whether to render a transcript, timeline, trace tree, approval queue, or debug log.</p><h2 id="correlation-fields" tabindex="-1">Correlation Fields <a class="header-anchor" href="#correlation-fields" aria-label="Permalink to &quot;Correlation Fields&quot;">​</a></h2><p>Use these fields to build projections:</p><table tabindex="0"><thead><tr><th>Field</th><th>Use it for</th></tr></thead><tbody><tr><td><code>SessionId</code> and <code>BranchId</code></td><td>Durable runtime scope and branch history lookup</td></tr><tr><td><code>EventFlowId</code></td><td>Branch/replay grouping, often a message-turn flow in persisted events</td></tr><tr><td><code>TraceId</code></td><td>Live execution trace for a message turn</td></tr><tr><td><code>SpanId</code> and <code>ParentSpanId</code></td><td>Span-style parent/child relationships when present</td></tr><tr><td><code>Metadata.AgentChain</code></td><td>Agent/workflow ancestry labels</td></tr><tr><td><code>Metadata.Depth</code></td><td>Nesting depth for agent-attributed events</td></tr><tr><td><code>MessageId</code></td><td>Text, reasoning, and tool activity under one assistant message</td></tr><tr><td><code>CallId</code></td><td>Tool-call lifecycle grouping</td></tr><tr><td><code>PermissionId</code></td><td>Permission request/response grouping</td></tr><tr><td><code>WorkflowName</code>, <code>AgentId</code>, <code>LayerIndex</code>, <code>FromNodeId</code>, <code>ToNodeId</code></td><td>Workflow timeline and graph rendering</td></tr></tbody></table><p><code>TraceId</code>, <code>SpanId</code>, and <code>ParentSpanId</code> are strongest for turn, iteration, and tool-start hierarchy. Not every event has a span. Tool args, result, and end events should usually be grouped with their <code>CallId</code>. <code>EventFlowId</code> is useful for branch history and replay, but it is not the same thing as span hierarchy.</p><h2 id="common-projections" tabindex="-1">Common Projections <a class="header-anchor" href="#common-projections" aria-label="Permalink to &quot;Common Projections&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Projection</th><th>Primary fields</th></tr></thead><tbody><tr><td>Chat transcript</td><td><code>MessageId</code>, text/reasoning start/delta/end events</td></tr><tr><td>Tool activity</td><td><code>CallId</code>, <code>ToolHarnessName</code>, <code>CallType</code>, <code>Name</code></td></tr><tr><td>Permission queue</td><td><code>PermissionId</code>, <code>CallId</code>, <code>FunctionName</code>, interactive channel</td></tr><tr><td>Workflow timeline</td><td><code>WorkflowName</code>, workflow node ids, layer events, edge events</td></tr><tr><td>Agent tree</td><td><code>Metadata.AgentChain</code>, <code>Metadata.Depth</code>, <code>Metadata.ParentAgentId</code></td></tr><tr><td>Trace view</td><td><code>TraceId</code>, <code>SpanId</code>, <code>ParentSpanId</code></td></tr><tr><td>Branch replay</td><td><code>SessionId</code>, <code>BranchId</code>, <code>EventFlowId</code>, branch event sequence</td></tr></tbody></table><p>For most product UIs, start with transcript and tool activity. Add workflow and agent projections only when the app actually exposes workflows, subagents, or multi-agent capabilities.</p><h2 id="workflows-and-subagents" tabindex="-1">Workflows And Subagents <a class="header-anchor" href="#workflows-and-subagents" aria-label="Permalink to &quot;Workflows And Subagents&quot;">​</a></h2><p>Subagents and workflow capabilities are still tools from the parent agent&#39;s point of view. Their parent run can emit normal tool events with <code>CallType</code> set to <code>SubAgent</code> or <code>MultiAgent</code>. The child execution can also emit its own events into the parent event path when the runtime links the child coordinator to the parent coordinator.</p><p>Workflows add dedicated workflow events such as:</p><ul><li><code>WorkflowStartedEvent</code></li><li><code>WorkflowCompletedEvent</code></li><li><code>WorkflowAgentStartedEvent</code></li><li><code>WorkflowAgentCompletedEvent</code></li><li><code>WorkflowAgentSkippedEvent</code></li><li><code>WorkflowEdgeTraversedEvent</code></li><li><code>WorkflowLayerStartedEvent</code></li><li><code>WorkflowLayerCompletedEvent</code></li><li><code>WorkflowDiagnosticEvent</code></li></ul><p>Generated workflow and subagent wrappers forward parent context for event coordination. Keep exact model-mediated permission and child-event assertions in your own integration tests until your app validates the full flow it depends on.</p><h2 id="live-events-and-durable-history" tabindex="-1">Live Events And Durable History <a class="header-anchor" href="#live-events-and-durable-history" aria-label="Permalink to &quot;Live Events And Durable History&quot;">​</a></h2><p>Live events and durable branch events are related, but they are not the same surface.</p><p>Live streams are for rendering current activity and handling interactive requests. Durable branch events are for replaying branch history and reconstructing branch projections. A live event may be important to a UI without being written to branch history.</p><p>Subagent durable history also depends on the subagent execution policy. A child may use the parent session, a fresh branch, a forked branch, an existing branch, or a new session. Document product behavior in terms of the policy you chose rather than assuming all child events persist on the parent branch.</p><h2 id="rendering-rule" tabindex="-1">Rendering Rule <a class="header-anchor" href="#rendering-rule" aria-label="Permalink to &quot;Rendering Rule&quot;">​</a></h2><p>Prefer this order when building a nested view:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>session + branch</span></span>
<span class="line"><span>  event flow or trace</span></span>
<span class="line"><span>    agent/workflow metadata</span></span>
<span class="line"><span>      message</span></span>
<span class="line"><span>        tool call</span></span>
<span class="line"><span>          interactive request</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>When a field is missing, fall back to the nearest useful parent. For example, a <code>ToolCallResultEvent</code> may not have span data, but it has <code>CallId</code>, so attach it to the matching <code>ToolCallStartEvent</code>.</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./../getting-started/streaming-events">Streaming Events</a></li><li><a href="./../guides/sessions-and-streaming/render-an-event-stream">Render An Event Stream</a></li><li><a href="./../guides/multi-agent/workflow-events">Workflow Events</a></li><li><a href="./../reference/events">Events Reference</a></li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("concepts/event-streams-and-hierarchies.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const eventStreamsAndHierarchies = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  eventStreamsAndHierarchies as default
};
