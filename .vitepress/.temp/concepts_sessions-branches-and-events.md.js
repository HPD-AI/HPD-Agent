import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Sessions, Branches, And Events","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/sessions-branches-and-events.md","filePath":"concepts/sessions-branches-and-events.md","lastUpdated":null}');
const _sfc_main = { name: "concepts/sessions-branches-and-events.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="sessions-branches-and-events" tabindex="-1">Sessions, Branches, And Events <a class="header-anchor" href="#sessions-branches-and-events" aria-label="Permalink to &quot;Sessions, Branches, And Events&quot;">​</a></h1><p>HPD Agent uses sessions, branches, and events as the durable coordination model for agent work. They are not only chat-history storage. The same model is used by normal turns, hosted runtimes, subagents, multi-agent workflows, content projection, compaction, replay, and UI navigation.</p><p>The short model is:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>session: the workspace container for metadata and shared middleware state</span></span>
<span class="line"><span>branch: a replayable transcript path with branch state, tree metadata, and event history</span></span>
<span class="line"><span>event: the live and durable vocabulary for rendering, replay, tools, middleware, and nested work</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>In direct in-process code, the built <code>Agent</code> owns the runtime and methods such as <code>CreateSessionAsync(...)</code>, <code>RunAsync(...)</code>, and <code>ForkBranchAsync(...)</code> operate against the configured session store.</p><p>In ASP.NET Core hosted applications, a runtime scope is selected by:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>agentId + sessionId + branchId</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>The <code>agentId</code> selects the hosted agent definition or runtime build path. The <code>sessionId</code> and <code>branchId</code> select the state and branch path used by that runtime agent.</p><h2 id="sessions" tabindex="-1">Sessions <a class="header-anchor" href="#sessions" aria-label="Permalink to &quot;Sessions&quot;">​</a></h2><p>A session is the top-level durable scope for an interaction. It has:</p><ul><li>an id</li><li>creation and last-activity timestamps</li><li>metadata</li><li>session-scoped middleware state</li></ul><p>Creating a session also creates the default branch named <code>main</code>. Session metadata updates are merge patches: new keys are added, existing keys are overwritten, and null values remove keys.</p><p>Use sessions for identity, grouping, UI lists, and state that should be shared across branches. For example, permission choices that apply across a user&#39;s branch tree are session-scoped middleware state.</p><p>A session can also represent a product workspace. Bots may map a platform thread to one session. A multi-agent workflow can stamp session metadata such as <code>workspaceKind</code>, <code>workflowName</code>, <code>workflowExecutionId</code>, and <code>conversationMode</code> so a UI can list the whole workflow run without loading every branch. A normal single-agent chat might use simpler metadata such as customer id, project id, or platform key.</p><h2 id="branches" tabindex="-1">Branches <a class="header-anchor" href="#branches" aria-label="Permalink to &quot;Branches&quot;">​</a></h2><p>A branch is the replayable path inside a session. A branch has:</p><ul><li>an id</li><li>optional name, description, tags, and metadata</li><li>fork metadata such as parent branch, fork point, ancestors, sibling position, and total forks</li><li>branch-scoped middleware state</li><li>a durable branch event log</li></ul><p>Branches are the unit for forking, replay, branch-specific state, and hosted runtime ownership. The default <code>main</code> branch is protected from deletion.</p><p>Forking creates a new branch from a message id on an existing branch. Branch-scoped persistent middleware state is copied at fork time and then diverges between branches.</p><p>Branches are also how HPD keeps nested agent work understandable. A subagent can use the parent branch, create a fresh branch, or fork from the parent branch. The default subagent policy forks from the parent session into a hidden child branch, so the child gets the parent&#39;s current context without interleaving its transcript with the parent. The child branch metadata points back to the parent session, parent branch, tool call, and subagent run.</p><p>Multi-agent workflows use the same branch model, but the application chooses the policy. A workflow can write every node into one shared branch, give each node its own branch, or fork one branch per node from a root workflow branch. Branch metadata marks the workflow name, execution id, node id, agent id, and conversation mode, which gives UI and replay code something stable to group around.</p><p>Media-heavy features make this branch scope visible. For example, audio transcript projection, content uploads, and assistant audio artifacts use the active <code>sessionId + branchId</code> so replay and forks can keep durable text, media references, and artifacts attached to the correct path. See <a href="./../guides/content/content-upload-and-resolution">Content Upload And Resolution</a> for the upload and resolver flow.</p><h2 id="how-features-use-the-model" tabindex="-1">How Features Use The Model <a class="header-anchor" href="#how-features-use-the-model" aria-label="Permalink to &quot;How Features Use The Model&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Feature</th><th>Session Use</th><th>Branch Use</th></tr></thead><tbody><tr><td>Normal chat</td><td>User, case, project, or platform thread container</td><td>Active transcript and branch-scoped middleware state</td></tr><tr><td>Forking</td><td>Keeps alternatives grouped together</td><td>Creates a new path from a message id</td></tr><tr><td>Subagents</td><td>Parent, new, or shared session depending on policy</td><td>Parent, existing, fresh, or forked child branch</td></tr><tr><td>Multi-agent workflows</td><td>Workflow/workspace container with run metadata</td><td>Shared, per-agent, or forked node transcripts</td></tr><tr><td>Compaction</td><td>Shared policy and session-scoped state can remain stable</td><td>Model-visible history, durable history, or fork target can be reduced</td></tr><tr><td>Content and audio</td><td>Groups durable references by workspace/session</td><td>Attaches uploads, artifacts, transcript projection, and replay to one path</td></tr><tr><td>Hosted runtimes</td><td><code>agentId + sessionId</code> selects the runtime scope</td><td><code>branchId</code> selects the active path and branch run</td></tr></tbody></table><h2 id="branch-history-projection" tabindex="-1">Branch History Projection <a class="header-anchor" href="#branch-history-projection" aria-label="Permalink to &quot;Branch History Projection&quot;">​</a></h2><p>Branches load as projections from durable branch events. The branch event log is the ordered record; the projected branch is the current messages, branch state, and metadata rebuilt from that record.</p><p>Branch-history compaction is one projection event family. Default soft compaction preserves durable branch history while reducing what the next model sees. Hard branch-history compaction can remove durable messages from the projected branch and can insert replacement messages such as summaries.</p><p>Fork compaction is especially important for nested work. A subagent or multi-agent node can fork from a rich parent/root branch while starting from a smaller model-visible history. That lets a child or node inherit the useful context without copying every durable message into its prompt.</p><h2 id="events" tabindex="-1">Events <a class="header-anchor" href="#events" aria-label="Permalink to &quot;Events&quot;">​</a></h2><p>Events are the framework vocabulary for input, streaming output, lifecycle, tool calls, interactive middleware, retry, background work, and branch-run tracking.</p><p>There are two related event surfaces:</p><ul><li>live agent events, used by local subscriptions, hosted SSE, WebSocket, TUI runtimes, and bot adapters</li><li>durable branch events, used to reconstruct branch history and branch projections</li></ul><p>Live event envelopes can include routing and correlation fields such as <code>sessionId</code>, <code>branchId</code>, <code>channel</code>, <code>direction</code>, trace ids, and metadata when those values are present. Durable branch event JSON is intentionally different and omits many live-routing fields.</p><p>Not every live event is written to branch history. Persistence is controlled by each event type&#39;s persistence policy and branch-event conversion, not by the event&#39;s channel.</p><p>This distinction matters for nested agents and workflows. A parent live stream can show subagent or workflow activity as it happens, while the durable transcript may live on a child branch or workflow node branch. Use event metadata for live hierarchy, and use session/branch metadata for durable lookup.</p><h2 id="runtime-scope-across-hosts" tabindex="-1">Runtime Scope Across Hosts <a class="header-anchor" href="#runtime-scope-across-hosts" aria-label="Permalink to &quot;Runtime Scope Across Hosts&quot;">​</a></h2><p>The same model applies across runtime surfaces:</p><ul><li>local agents use the same event vocabulary through typed subscriptions</li><li>hosted APIs expose sessions, branches, branch runs, inputs, SSE, WebSocket, and the bidirectional response route</li><li>TUI runtimes bind UI state to a runtime scope</li><li>bot adapters map platform conversation identity to a session and branch before invoking an agent</li><li>subagents use policy to decide whether child work stays on the parent branch, forks, or moves to another session</li><li>multi-agent workflows use conversation policy to decide where node-agent transcripts are written</li></ul><p>This means UI and integration code should treat events as the common rendering and coordination language, while treating hosting routes and local subscriptions as transport choices.</p><h2 id="concurrency-model" tabindex="-1">Concurrency Model <a class="header-anchor" href="#concurrency-model" aria-label="Permalink to &quot;Concurrency Model&quot;">​</a></h2><p>Branch runs and branch operation locks are separate controls.</p><p>A hosted branch run is the active model/tool execution for a branch. Only one active hosted run is allowed for a given <code>sessionId + branchId</code>, regardless of route <code>agentId</code>. A second simultaneous input submission to the same branch returns a conflict, while submissions to different branches can run at the same time.</p><p>A branch operation lock protects exclusive branch mutations. Current hosting code uses the explicit branch operation lock for branch deletion. Some other branch operations use broader session-level coordination. Treat operation-lock conflicts as branch mutation conflicts, not as active agent runs.</p><p>Middleware responses are also separate from branch runs. A permission, continuation, clarification, or client-tool response is accepted only when the target branch runtime is active and a waiter can accept that response.</p><p>Shared branch policies need additional coordination. If two agents write to the same durable branch at once, their branch snapshots and branch middleware state can race. HPD serializes same-branch multi-agent node runs at the conversation route. Separate branches can still run in parallel.</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./../guides/sessions-and-streaming/branch-history-and-forking">Branch History And Forking</a></li><li><a href="./../guides/sessions-and-streaming/compaction">Compaction</a></li><li><a href="./../guides/agents/subagents">Subagents</a></li><li><a href="./../guides/multi-agent/conversation-policies">Multi-Agent Conversation Policies</a></li><li><a href="./../guides/hosting/hosted-streaming-api">Hosted Streaming API</a></li><li><a href="./../reference/hosted-endpoints">Hosted Endpoints</a></li><li><a href="./../reference/events">Events Reference</a></li><li><a href="./../guides/audio/overview">Audio Overview</a></li><li><a href="./../guides/evaluations/live-evaluation">Live Evaluation</a></li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("concepts/sessions-branches-and-events.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const sessionsBranchesAndEvents = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  sessionsBranchesAndEvents as default
};
