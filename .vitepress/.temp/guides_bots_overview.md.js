import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Bots Overview","description":"","frontmatter":{},"headers":[],"relativePath":"guides/bots/overview.md","filePath":"guides/bots/overview.md","lastUpdated":null}');
const _sfc_main = { name: "guides/bots/overview.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="bots-overview" tabindex="-1">Bots Overview <a class="header-anchor" href="#bots-overview" aria-label="Permalink to &quot;Bots Overview&quot;">​</a></h1><p>HPD bot adapters connect platform messages to HPD agent sessions and branches. Use the bot packages when you want Slack, Discord, Telegram, WhatsApp, or Teams to drive the same hosted HPD agent runtime that your HTTP and UI surfaces use.</p><p>Start with <a href="./platform-setup">Platform Setup</a> when you are wiring a real bot. Use this page when you want to understand how all adapters map platform conversations into HPD sessions.</p><p>Provider pages:</p><ul><li><a href="./slack">Slack Bots</a></li><li><a href="./discord">Discord Bots</a></li><li><a href="./telegram">Telegram Bots</a></li><li><a href="./whatsapp">WhatsApp Bots</a></li><li><a href="./teams">Teams Bots</a></li></ul><h2 id="runtime-flow" tabindex="-1">Runtime Flow <a class="header-anchor" href="#runtime-flow" aria-label="Permalink to &quot;Runtime Flow&quot;">​</a></h2><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>platform event, update, interaction, or activity</span></span>
<span class="line"><span>  -&gt; platform-specific thread key</span></span>
<span class="line"><span>  -&gt; PlatformSessionMapper.ResolveAsync(platformKey)</span></span>
<span class="line"><span>  -&gt; HPD sessionId + branchId</span></span>
<span class="line"><span>  -&gt; AgentManager builds or gets the configured agent</span></span>
<span class="line"><span>  -&gt; Agent.RunAsync(...) in that session and branch scope</span></span>
<span class="line"><span>  -&gt; platform-specific streaming/output callbacks</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>The platform adapter owns incoming transport details. HPD owns the agent run, session, branch, event stream, tools, and middleware behavior.</p><p>Most hosted examples use ASP.NET because webhooks are the common deployment shape. The adapter core is transport-neutral: generated adapters implement <code>IBotAdapter.HandleAsync(BotInboundEnvelope, ...)</code>, and the ASP.NET route is a generated bridge that converts <code>HttpContext</code> into that envelope. Polling, socket, worker, or custom hosts can dispatch the same adapter without making the bot logic depend on ASP.NET.</p><h2 id="platform-keys" tabindex="-1">Platform Keys <a class="header-anchor" href="#platform-keys" aria-label="Permalink to &quot;Platform Keys&quot;">​</a></h2><p>A platform key is an opaque string that identifies a conversation thread from the platform&#39;s point of view. Examples include a Slack channel and thread timestamp, a Discord interaction/thread identity, a Telegram chat/topic identity, a WhatsApp phone/user pair, or a Teams conversation identity.</p><p><code>PlatformSessionMapper</code> maps that key to an HPD session and branch. On a miss, it creates a session through <code>SessionManager</code>, which also creates the default branch.</p><p>The mapper stores the primary key in session metadata as <code>platformKey</code>. It can also bind aliases through <code>platformKeyAliases</code> when a platform changes thread identity after the initial message.</p><h2 id="scaling-caveat" tabindex="-1">Scaling Caveat <a class="header-anchor" href="#scaling-caveat" aria-label="Permalink to &quot;Scaling Caveat&quot;">​</a></h2><p>The current mapper searches existing sessions by listing session ids and loading each session to compare metadata. That is O(n) over sessions. This is acceptable for low-volume deployments and early integrations, but high-volume bots should plan for a secondary index, cache, or store-level lookup before relying on this path at scale.</p><h2 id="agent-selection" tabindex="-1">Agent Selection <a class="header-anchor" href="#agent-selection" aria-label="Permalink to &quot;Agent Selection&quot;">​</a></h2><p>Platform configuration resolves an HPD agent id. That id selects the hosted or managed agent definition that handles the conversation.</p><p>Keep this separate from the platform thread key:</p><ul><li>the platform key identifies the user conversation</li><li>the agent id identifies which HPD agent should answer</li><li>the session id and branch id identify the persisted HPD conversation path</li></ul><p>Most concrete adapters require <code>AgentId</code> in their config. That value should name the agent definition you want the platform to invoke, not the platform app id, bot user id, or channel id.</p><h2 id="adapter-packages" tabindex="-1">Adapter Packages <a class="header-anchor" href="#adapter-packages" aria-label="Permalink to &quot;Adapter Packages&quot;">​</a></h2><p>The concrete packages provide hand-written setup extensions around the generated adapter registration:</p><ul><li>Slack: <code>AddSlackBot(...)</code>, <code>MapSlackWebhook(...)</code>, optional <code>AddSlackOAuth(...)</code>, optional Socket Mode with <code>AppToken</code>.</li><li>Discord: <code>AddDiscordBot(...)</code>, <code>MapDiscordWebhook(...)</code>, optional Gateway forwarding.</li><li>Telegram: <code>AddTelegramBot(...)</code> with <code>MapTelegramWebhook(...)</code>, or <code>AddTelegramBotWithPolling(...)</code> without a public webhook.</li><li>WhatsApp: <code>AddWhatsappBot(...)</code>, <code>MapWhatsappWebhook(...)</code>.</li><li>Teams: <code>builder.AddTeamsBot(...)</code>, <code>app.MapTeamsBot()</code>.</li></ul><p>Prefer these package extensions over the bare generated overloads because they register platform HTTP clients, API clients, format converters, socket or polling services, registry providers, and other infrastructure.</p><h2 id="streaming-runner" tabindex="-1">Streaming Runner <a class="header-anchor" href="#streaming-runner" aria-label="Permalink to &quot;Streaming Runner&quot;">​</a></h2><p>Adapters can use <code>BotStreamingRunner</code> to run an agent and send output back through platform callbacks. The runner obtains a branch operation lock, builds or gets the configured agent, subscribes to events, runs the scoped input, and releases the lock when the run finishes.</p><p>The shared output callbacks cover text deltas, final text, cards, and permission requests. Each platform still decides how those primitives become platform-native messages, edits, cards, buttons, or no-op behavior.</p><h2 id="permission-caveats" tabindex="-1">Permission Caveats <a class="header-anchor" href="#permission-caveats" aria-label="Permalink to &quot;Permission Caveats&quot;">​</a></h2><p>Do not assume every platform has the same approval UX.</p><p>Current platform behavior is uneven. Some adapters may render interactive approval controls, while others may deny permission requests automatically or use platform-specific action handling. Platform button responses are only meaningful while the corresponding agent run is still waiting for the response.</p><p>Bot permission responses should not be described as hosted TUI response-route calls. They are handled through the bot adapter and active agent runtime path, not by posting to the hosted TUI&#39;s HTTP response route.</p><h2 id="custom-adapters" tabindex="-1">Custom Adapters <a class="header-anchor" href="#custom-adapters" aria-label="Permalink to &quot;Custom Adapters&quot;">​</a></h2><p>When a platform package does not exist, use <a href="./custom-adapters-and-source-generation">Custom Bot Adapters And Source Generation</a>. The generated source handles the repeated adapter dispatch shape and can also emit ASP.NET mapping helpers, but the adapter package still owns auth, signature verification, payload parsing, thread-key design, platform output, and any SDK-specific services.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guides/bots/overview.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const overview = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  overview as default
};
