import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Branch History And Forking","description":"","frontmatter":{},"headers":[],"relativePath":"guides/sessions-and-streaming/branch-history-and-forking.md","filePath":"guides/sessions-and-streaming/branch-history-and-forking.md","lastUpdated":null}');
const _sfc_main = { name: "guides/sessions-and-streaming/branch-history-and-forking.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="branch-history-and-forking" tabindex="-1">Branch History And Forking <a class="header-anchor" href="#branch-history-and-forking" aria-label="Permalink to &quot;Branch History And Forking&quot;">​</a></h1><p>Branches let one session hold multiple replayable paths. A branch can be created directly, forked from a message id, updated for UI metadata, listed with sibling/tree information, and deleted subject to branch protection rules.</p><p>There are two public ways to work with branches:</p><ul><li>Direct in-process <code>Agent</code> APIs, where your code calls methods on a built <code>Agent</code> that has a session store.</li><li>ASP.NET Core hosted APIs, where clients use HTTP routes and DTOs exposed by <code>MapHPDAgentApi(...)</code>.</li></ul><h2 id="direct-agent-api" tabindex="-1">Direct Agent API <a class="header-anchor" href="#direct-agent-api" aria-label="Permalink to &quot;Direct Agent API&quot;">​</a></h2><p>Use direct APIs when your application owns the <code>Agent</code> instance:</p><div class="language-csharp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">csharp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">var</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> sessionId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> await</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> agent.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">CreateSessionAsync</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;review-session&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">);</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">await</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> agent.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">RunAsync</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">    &quot;Draft the first answer.&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">    sessionId</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: sessionId,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">    branchId</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;main&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">);</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">var</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> forkId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> await</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> agent.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ForkBranchAsync</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    sessionId,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">    sourceBranchId</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;main&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">    newBranchId</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;short-answer&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">    fromMessageId</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: messageId);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br></div></div><p>Direct APIs require a configured session store. <code>CreateSessionAsync(...)</code> creates the default <code>main</code> branch. <code>ForkBranchAsync(...)</code> returns the new branch id and can also accept <code>BranchForkOptions</code> for programmatic fork behavior such as fork compaction.</p><h2 id="asp-net-core-hosted-routes" tabindex="-1">ASP.NET Core Hosted Routes <a class="header-anchor" href="#asp-net-core-hosted-routes" aria-label="Permalink to &quot;ASP.NET Core Hosted Routes&quot;">​</a></h2><p>Branch read, update, delete, content, and event-log routes are scoped by <code>sessionId</code> and <code>branchId</code>. Branch create and fork routes also include <code>agentId</code> because they need the hosted runtime configuration for that agent scope.</p><table tabindex="0"><thead><tr><th>Operation</th><th>Route</th></tr></thead><tbody><tr><td>List branches</td><td><code>GET /sessions/{sessionId}/branches</code></td></tr><tr><td>Get branch</td><td><code>GET /sessions/{sessionId}/branches/{branchId}</code></td></tr><tr><td>Create branch</td><td><code>POST /agents/{agentId}/sessions/{sessionId}/branches</code></td></tr><tr><td>Fork branch</td><td><code>POST /agents/{agentId}/sessions/{sessionId}/branches/{branchId}/fork</code></td></tr><tr><td>Update branch</td><td><code>PATCH /sessions/{sessionId}/branches/{branchId}</code></td></tr><tr><td>Delete branch</td><td><code>DELETE /sessions/{sessionId}/branches/{branchId}?recursive=false</code></td></tr><tr><td>Get branch events</td><td><code>GET /sessions/{sessionId}/branches/{branchId}/events</code></td></tr><tr><td>Get siblings</td><td><code>GET /sessions/{sessionId}/branches/{branchId}/siblings</code></td></tr></tbody></table><h2 id="hosted-create-a-branch" tabindex="-1">Hosted Create A Branch <a class="header-anchor" href="#hosted-create-a-branch" aria-label="Permalink to &quot;Hosted Create A Branch&quot;">​</a></h2><p>Creating a branch accepts a <code>CreateBranchRequest</code> with a branch id plus optional name, description, tags, and metadata. If the branch id is blank, the service can generate one.</p><p>Creating a branch with an existing id returns a conflict. A branch created this way starts as its own path rather than a fork from another branch.</p><h2 id="hosted-fork-from-a-message" tabindex="-1">Hosted Fork From A Message <a class="header-anchor" href="#hosted-fork-from-a-message" aria-label="Permalink to &quot;Hosted Fork From A Message&quot;">​</a></h2><p>Forking accepts a <code>ForkBranchRequest</code>:</p><div class="language-json vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;newBranchId&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;try-short-answer&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;fromMessageId&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;message-id-to-fork-from&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;name&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;Short answer&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;description&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;A shorter response path&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;tags&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;draft&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;metadata&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    &quot;uiColor&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;green&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p><code>fromMessageId</code> is required. If the message id is not present on the source branch, the hosted API returns a validation error.</p><p>The new branch records its source branch and fork point. Persistent branch-scoped middleware state is copied to the new branch when the fork is committed and then evolves independently.</p><h2 id="hosted-update-branch-metadata" tabindex="-1">Hosted Update Branch Metadata <a class="header-anchor" href="#hosted-update-branch-metadata" aria-label="Permalink to &quot;Hosted Update Branch Metadata&quot;">​</a></h2><p>Branch updates accept optional name, description, tags, and metadata. Metadata is merged: values add or overwrite keys, and null removes keys.</p><p>Use branch metadata for UI labels, filters, annotations, and runtime hints that belong to a single path.</p><h2 id="delete-branches" tabindex="-1">Delete Branches <a class="header-anchor" href="#delete-branches" aria-label="Permalink to &quot;Delete Branches&quot;">​</a></h2><p>The <code>main</code> branch is protected and cannot be deleted.</p><p>In the hosted API, deleting a branch with children returns a conflict unless <code>recursive=true</code>. Recursive deletion must also be enabled by server configuration. If recursive deletion is disabled, the API returns a validation error even when the request includes <code>recursive=true</code>.</p><p>Hosted branch deletion uses a branch operation lock. A lock conflict means another exclusive branch mutation is in progress; it does not mean an agent run is active.</p><h2 id="read-branch-events" tabindex="-1">Read Branch Events <a class="header-anchor" href="#read-branch-events" aria-label="Permalink to &quot;Read Branch Events&quot;">​</a></h2><p><code>GET /sessions/{sessionId}/branches/{branchId}/events</code> returns normalized polymorphic <code>AgentEvent</code> JSON for the branch log. This is useful for rebuilding branch views and debugging history.</p><p>Do not assume the branch event JSON is identical to live SSE or WebSocket event envelopes. Live event envelopes can include routing and correlation fields when they are present on the event. Durable branch event JSON may omit live-routing fields and only contains events that were mapped or opted into branch persistence.</p><p>In direct in-process code, read branch history through the configured session store or higher-level runtime that wraps it. In ASP.NET Core hosted clients, use the branch events route.</p><p>For UI projection guidance, see <a href="./render-an-event-stream">Render An Event Stream</a>. For the live-vs-durable model, see <a href="./../../concepts/event-streams-and-hierarchies">Event Streams And Hierarchies</a>.</p><h2 id="branch-history-compaction" tabindex="-1">Branch History Compaction <a class="header-anchor" href="#branch-history-compaction" aria-label="Permalink to &quot;Branch History Compaction&quot;">​</a></h2><p>Hard branch-history compaction changes the projected branch. A <code>BRANCH_HISTORY_COMPACTED</code> durable branch event removes compacted durable message ids from projection and inserts replacement messages when the retention mode produced them.</p><p>Soft compaction does not change the durable branch projection. It only reduces what the next model turn sees.</p><p>For the full model, see <a href="./compaction">Compaction</a>.</p><h2 id="forking-after-compaction" tabindex="-1">Forking After Compaction <a class="header-anchor" href="#forking-after-compaction" aria-label="Permalink to &quot;Forking After Compaction&quot;">​</a></h2><p>Forking uses a message id on the projected source branch. If hard compaction removed an older message from the projection, that original message id may no longer be a valid fork point. The framework can surface replacement candidates when a compacted-away id is detected, but clients should not assume automatic recovery.</p><p>Use projected branch messages as the source of forkable message ids.</p><h2 id="fork-and-compact" tabindex="-1">Fork And Compact <a class="header-anchor" href="#fork-and-compact" aria-label="Permalink to &quot;Fork And Compact&quot;">​</a></h2><p>Fork compaction is a separate pre-commit behavior. When configured, the target branch is compacted after it is copied from the source branch and before the target branch is saved.</p><p>The source branch is unchanged. The target branch starts with the already-compacted initial history. Fork compaction does not write a standalone <code>BRANCH_HISTORY_COMPACTED</code> event.</p><p>Hosted <code>ForkBranchRequest</code> does not expose a per-request compaction intent today. Hosted fork compaction is controlled by server-side agent and middleware configuration.</p><p>The middleware hook for this phase is <code>BeforeBranchForkCommitAsync</code>. It receives the source branch, the in-memory target branch, the fork point, and <code>BranchForkOptions</code>. This is the place for middleware that needs to change the target branch before it becomes durable. After the fork is committed, durable branch events such as <code>BRANCH_FORKED</code> describe the committed branch state; they are not mutation hooks.</p><h2 id="sibling-navigation" tabindex="-1">Sibling Navigation <a class="header-anchor" href="#sibling-navigation" aria-label="Permalink to &quot;Sibling Navigation&quot;">​</a></h2><p>The hosted sibling endpoint returns branch DTOs with tree and sibling fields such as sibling index, total siblings, previous sibling id, next sibling id, original branch id, and total forks.</p><p>Current implementation returns full <code>BranchDto</code> values for siblings. Avoid depending on a separate lightweight sibling DTO until that contract is reconciled.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guides/sessions-and-streaming/branch-history-and-forking.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const branchHistoryAndForking = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  branchHistoryAndForking as default
};
