import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Events","description":"","frontmatter":{},"headers":[],"relativePath":"reference/events.md","filePath":"reference/events.md","lastUpdated":null}');
const _sfc_main = { name: "reference/events.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="events" tabindex="-1">Events <a class="header-anchor" href="#events" aria-label="Permalink to &quot;Events&quot;">​</a></h1><p>Events are the common vocabulary for HPD Agent runtime activity. Local subscriptions, hosted SSE, hosted WebSocket, TUI runtimes, bot adapters, middleware, and branch history all use related event types.</p><p>This page is family-based. It is not an exhaustive generated schema reference.</p><h2 id="live-event-envelope" tabindex="-1">Live Event Envelope <a class="header-anchor" href="#live-event-envelope" aria-label="Permalink to &quot;Live Event Envelope&quot;">​</a></h2><p>Hosted SSE and WebSocket use <code>AgentEventSerializer</code> envelopes:</p><div class="language-json vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;version&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;1.0&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;type&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;TEXT_DELTA&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;text&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;hello&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;messageId&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;message-id&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>The envelope includes injected <code>version</code> and <code>type</code> fields. Built-in type names use <code>SCREAMING_SNAKE_CASE</code>. Payload properties are camelCase and null values are omitted.</p><p>Unknown or missing <code>type</code> values cannot be deserialized as known events. HTTP input treats invalid event envelopes as bad requests. WebSocket closes invalid event frames with an invalid-payload close status.</p><p>Custom event types must be registered with the event serializer, normally through source generation, and need JSON metadata for AOT-safe serialization. See <a href="./../guides/events/custom-events">Custom Events</a>.</p><p>For the practical serialization path, see <a href="./../guides/events/serialization-and-registration">Serialization And Registration</a>.</p><h2 id="channels-direction-and-persistence" tabindex="-1">Channels, Direction, And Persistence <a class="header-anchor" href="#channels-direction-and-persistence" aria-label="Permalink to &quot;Channels, Direction, And Persistence&quot;">​</a></h2><p>Event channel and direction help route and render events. They are not persistence guarantees.</p><p>Common channel meanings:</p><table tabindex="0"><thead><tr><th>Channel</th><th>Typical use</th></tr></thead><tbody><tr><td>Streaming</td><td>text and reasoning deltas</td></tr><tr><td>Interactive</td><td>permission, continuation, clarification, and client-tool request/response flows</td></tr><tr><td>Control</td><td>branch-run lifecycle and interruption handling</td></tr><tr><td>Default or inherited</td><td>lifecycle, diagnostic, and framework-specific events that do not explicitly set a channel</td></tr></tbody></table><p>Response events for permission, continuation, and clarification use upstream direction.</p><p>Branch persistence is separate. Events are written to branch history only when branch conversion maps them or the event type opts in to branch persistence. A live event may be important to a UI without being durable branch history.</p><h2 id="correlation-and-projection-fields" tabindex="-1">Correlation And Projection Fields <a class="header-anchor" href="#correlation-and-projection-fields" aria-label="Permalink to &quot;Correlation And Projection Fields&quot;">​</a></h2><p>Events arrive as a stream. Clients can project that stream into transcripts, timelines, workflow views, prompt queues, or trace trees by using correlation fields when they are present.</p><p>Common fields:</p><table tabindex="0"><thead><tr><th>Field</th><th>Meaning</th></tr></thead><tbody><tr><td><code>sessionId</code>, <code>branchId</code></td><td>Durable runtime scope when present</td></tr><tr><td><code>eventFlowId</code></td><td>Flow grouping used by branch history and replay; often a message-turn flow for persisted events</td></tr><tr><td><code>traceId</code></td><td>Live execution trace id for a message turn</td></tr><tr><td><code>spanId</code>, <code>parentSpanId</code></td><td>Span-style hierarchy when present</td></tr><tr><td><code>metadata</code></td><td>Agent attribution, including agent name, id, parent id, chain, and depth</td></tr><tr><td><code>messageId</code></td><td>Text, reasoning, and tool activity under a message</td></tr><tr><td><code>callId</code></td><td>Tool-call lifecycle grouping</td></tr><tr><td><code>permissionId</code> and other request ids</td><td>Bidirectional request/response grouping</td></tr></tbody></table><p>Do not require every event to have every field. For example, some tool events carry <code>traceId</code> and <code>callId</code> without explicit span fields. Use the most specific available id for the projection you are building. See <a href="./../concepts/event-streams-and-hierarchies">Event Streams And Hierarchies</a> and <a href="./../guides/sessions-and-streaming/render-an-event-stream">Render An Event Stream</a>.</p><h2 id="branch-event-json-caveat" tabindex="-1">Branch Event JSON Caveat <a class="header-anchor" href="#branch-event-json-caveat" aria-label="Permalink to &quot;Branch Event JSON Caveat&quot;">​</a></h2><p>Live event envelopes and durable branch event JSON are different surfaces.</p><p>Live envelopes always include <code>version</code> and <code>type</code>, and can include routing and correlation fields such as session id, branch id, channel, direction, event flow id, metadata, and trace/span fields when those values are present on the event.</p><p>Durable branch event JSON omits many of those fields. Use live envelopes for API/SSE/WebSocket examples. Use branch event documents only when documenting storage or branch projection behavior.</p><h2 id="event-families" tabindex="-1">Event Families <a class="header-anchor" href="#event-families" aria-label="Permalink to &quot;Event Families&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Family</th><th>Examples</th><th>Primary channel</th><th>Branch persistence</th></tr></thead><tbody><tr><td>Input</td><td><code>UserTextInputEvent</code>, <code>UserMessagesInputEvent</code>, <code>InterruptionRequestEvent</code></td><td>upstream/control</td><td>input handling surface; do not assume all are durable</td></tr><tr><td>Hosted branch run</td><td><code>BranchRunStartedEvent</code>, <code>BranchRunCompletedEvent</code></td><td>control</td><td>durable</td></tr><tr><td>Message turn lifecycle</td><td><code>MessageTurnStartedEvent</code>, <code>MessageTurnFinishedEvent</code>, <code>MessageTurnErrorEvent</code></td><td>default/control</td><td>start and finish are durable; failures may be represented through branch failure conversion</td></tr><tr><td>Agent turn lifecycle</td><td><code>AgentTurnStartedEvent</code>, <code>AgentTurnFinishedEvent</code></td><td>default</td><td>live/runtime-oriented</td></tr><tr><td>Text streaming</td><td><code>TextMessageStartEvent</code>, <code>TextDeltaEvent</code>, <code>TextMessageEndEvent</code></td><td>streaming</td><td>durable</td></tr><tr><td>Reasoning streaming</td><td><code>ReasoningMessageStartEvent</code>, <code>ReasoningDeltaEvent</code>, <code>ReasoningMessageEndEvent</code></td><td>streaming</td><td>durable</td></tr><tr><td>Tool calls</td><td><code>ToolCallStartEvent</code>, <code>ToolCallArgsEvent</code>, <code>ToolCallResultEvent</code>, <code>ToolCallEndEvent</code></td><td>default/streaming</td><td>durable</td></tr><tr><td>Workflow execution</td><td><code>WorkflowStartedEvent</code>, <code>WorkflowAgentStartedEvent</code>, <code>WorkflowEdgeTraversedEvent</code>, <code>WorkflowCompletedEvent</code></td><td>default/diagnostic</td><td>validate persistence for the workflow path</td></tr><tr><td>Background work</td><td><code>ToolCallBackgroundTask*</code>, <code>BackgroundOperationStartedEvent</code>, <code>BackgroundOperationStatusEvent</code></td><td>control/default</td><td>task events and selected operation statuses are durable</td></tr><tr><td>Interactive middleware</td><td>permission, continuation, clarification, client-tool request/response events</td><td>interactive</td><td>generally live only</td></tr><tr><td>Retry and error policy</td><td><code>FunctionRetryEvent</code>, <code>ModelCallRetryEvent</code>, middleware error events</td><td>diagnostic/default</td><td>generally live only</td></tr><tr><td>Compaction observability</td><td><code>CompactionEvent</code></td><td>default/diagnostic</td><td>live middleware event; not the durable projection instruction</td></tr><tr><td>Branch-history compaction</td><td><code>BranchHistoryCompactedEvent</code></td><td>branch history</td><td>durable when hard retention is applied</td></tr><tr><td>Content and references</td><td>upload/reference events</td><td>default/diagnostic</td><td>depends on event and content persistence policy</td></tr><tr><td>Audio transcripts</td><td><code>UserAudioTranscriptDeltaEvent</code>, <code>UserAudioTranscriptCompletedEvent</code>, <code>UserAudioTranscriptFailedEvent</code></td><td>streaming/default</td><td>transcript text projection is policy-dependent; raw audio is not durable by default</td></tr><tr><td>Assistant audio output</td><td>assistant audio output, stream, artifact, failure, and playback events</td><td>default/diagnostic</td><td>live/runtime-oriented unless explicitly projected by audio runtime policy</td></tr><tr><td>Struct events</td><td><code>AgentStructEvent</code> samples on <code>StructEventHub</code> routes</td><td>process-local/export</td><td>not hosted or durable by default</td></tr><tr><td>Planning, schema, structured output</td><td>framework diagnostics and state changes</td><td>default/diagnostic</td><td>event-specific</td></tr></tbody></table><p><code>CompactionStatus.CacheHit</code> exists as an enum value, but current public docs should not describe cached compaction reuse as active middleware behavior.</p><h2 id="struct-events" tabindex="-1">Struct Events <a class="header-anchor" href="#struct-events" aria-label="Permalink to &quot;Struct Events&quot;">​</a></h2><p>Struct events are a separate process-local surface for realtime samples. HPD Agent-owned samples implement <code>AgentStructEvent</code>, which is an agent-level marker over the lower HPD Events struct-event contract. They do not inherit from <code>AgentEvent</code>, do not use <code>AgentEventSerializer</code>, and do not appear in hosted SSE/WebSocket streams or branch history unless a component explicitly converts or summarizes them as an <code>AgentEvent</code>.</p><p>Selected struct events can be exported with <code>AgentStructEventSerializer</code>. That serializer uses a separate envelope from hosted <code>AgentEvent</code> streaming and is for diagnostics, local capture, replay tooling, or telemetry export. Serializing a struct event does not make it visible to the TypeScript client or durable branch history.</p><p>Use <code>agent.ObserveStruct&lt;TEvent&gt;(...)</code> or <code>agent.StructEvents.Route&lt;TEvent&gt;()</code> for local observation. Tools that accept <code>FunctionExecutionContext</code> can access <code>context.StructEvents</code> for the same process-local sample lanes. Use this for high-volume diagnostics such as audio playout samples or queue-depth telemetry, not for semantic workflow facts, permissions, or persisted progress.</p><h2 id="assistant-audio-output-events" tabindex="-1">Assistant Audio Output Events <a class="header-anchor" href="#assistant-audio-output-events" aria-label="Permalink to &quot;Assistant Audio Output Events&quot;">​</a></h2><p>Assistant audio output events use the same live envelope as other <code>AgentEvent</code> values. The event payload includes <code>sessionId</code> when the event has a session scope, plus audio-specific identifiers such as <code>outputFlowId</code>, <code>responseId</code>, <code>segmentId</code>, and provider or playback fields.</p><p>Source-registered assistant audio output type names include:</p><table tabindex="0"><thead><tr><th>Type</th><th>Purpose</th></tr></thead><tbody><tr><td><code>ASSISTANT_AUDIO_OUTPUT_STARTED</code></td><td>Assistant audio output flow started.</td></tr><tr><td><code>ASSISTANT_AUDIO_OUTPUT_STREAM_STARTED</code></td><td>A synthesized audio stream segment started.</td></tr><tr><td><code>ASSISTANT_AUDIO_OUTPUT_CHUNK_READY</code></td><td>An audio chunk is ready.</td></tr><tr><td><code>ASSISTANT_AUDIO_PUSH_TEXT_STREAM_OPENING</code></td><td>Push-text TTS stream is opening.</td></tr><tr><td><code>ASSISTANT_AUDIO_PUSH_TEXT_STREAM_OPENED</code></td><td>Push-text TTS stream opened.</td></tr><tr><td><code>ASSISTANT_AUDIO_PUSH_TEXT_INPUT_SENT</code></td><td>Text input was sent to a push-text TTS stream.</td></tr><tr><td><code>ASSISTANT_AUDIO_OUTPUT_STREAM_COMPLETED</code></td><td>An audio stream segment completed.</td></tr><tr><td><code>ASSISTANT_AUDIO_OUTPUT_ARTIFACT_CAPTURED</code></td><td>Audio output was captured as an artifact.</td></tr><tr><td><code>ASSISTANT_AUDIO_OUTPUT_SEGMENT_FAILED</code></td><td>One audio segment failed.</td></tr><tr><td><code>ASSISTANT_AUDIO_OUTPUT_COMPLETED</code></td><td>Assistant audio output flow completed.</td></tr><tr><td><code>ASSISTANT_AUDIO_OUTPUT_FAILED</code></td><td>Assistant audio output flow failed.</td></tr><tr><td><code>ASSISTANT_AUDIO_PLAYBACK_QUEUED</code></td><td>Audio playback was queued.</td></tr><tr><td><code>ASSISTANT_AUDIO_PLAYBACK_STARTED</code></td><td>Audio playback started.</td></tr><tr><td><code>ASSISTANT_AUDIO_PLAYBACK_PROGRESS</code></td><td>Audio playback progress was reported.</td></tr><tr><td><code>ASSISTANT_AUDIO_PLAYBACK_COMPLETED</code></td><td>Audio playback completed.</td></tr><tr><td><code>ASSISTANT_AUDIO_PLAYBACK_INTERRUPTED</code></td><td>Audio playback was interrupted.</td></tr><tr><td><code>ASSISTANT_AUDIO_PLAYBACK_FAILED</code></td><td>Audio playback failed.</td></tr></tbody></table><p>Value round-trip tests cover <code>sessionId</code>, output flow, response, segment, chunk, artifact, error, and playback fields for these event families.</p><h2 id="ui-rendering-guidance" tabindex="-1">UI Rendering Guidance <a class="header-anchor" href="#ui-rendering-guidance" aria-label="Permalink to &quot;UI Rendering Guidance&quot;">​</a></h2><p>For text output, render <code>TextMessageStartEvent</code>, append <code>TextDeltaEvent</code>, and close the block on <code>TextMessageEndEvent</code>.</p><p>For reasoning output, use the same start/delta/end pattern but render it separately from final user-visible text.</p><p>For tool calls, use start/args/result/end events to show activity, arguments, and results. Tool events may arrive interleaved with streaming text depending on the model and middleware.</p><p>For interactive middleware, render request events and answer with the matching response event through WebSocket, the hosted HTTP <code>/responses</code> route, or <code>agent.RespondAsync(...)</code> in a local app. Permission, continuation, clarification, and client-tool responses are coordination events, not ordinary output. See <a href="./../guides/events/bidirectional-events">Bidirectional Events</a>.</p><p>For JavaScript and TypeScript apps, use <code>AgentClient</code> to load branch history, subscribe to live events, handle unknown custom events through <code>onAny(...)</code>, and send response events back to the hosted runtime. See <a href="./../guides/events/typescript-client">TypeScript Client Events</a>.</p><p>For retry events, be careful with already-rendered partial output. A <code>ModelCallRetryEvent</code> can arrive after partial model content, so UIs may need to clear or mark stale partial text before rendering retry output.</p><p>For lifecycle, retry, and failure rendering, see <a href="./../guides/events/lifecycle-retry-and-error-events">Lifecycle, Retry, And Error Events</a>.</p><p>For branch runs, use branch-run started/completed events to track active hosted work. Do not use branch operation lock conflicts as branch-run state.</p><p>For compaction, distinguish live observability from durable projection. <code>CompactionEvent</code> can explain why middleware skipped or performed compaction. <code>BranchHistoryCompactedEvent</code> changes the branch projection by removing durable compacted messages and inserting replacements when present.</p><p>For workflows, render workflow lifecycle, layer, node, edge, and diagnostic events as a timeline or graph projection. Child agent, tool, and permission events may also appear in the same live stream when the workflow or subagent execution bubbles events through a parent coordinator.</p><p>For tool progress emitted from inside a function body, see <a href="./../guides/events/tool-and-function-events">Tool And Function Events</a>.</p><h2 id="persistence-summary" tabindex="-1">Persistence Summary <a class="header-anchor" href="#persistence-summary" aria-label="Permalink to &quot;Persistence Summary&quot;">​</a></h2><p>Persisting families verified for first docs:</p><ul><li>branch-run started and completed</li><li>message-turn started and finished</li><li>text streaming start, delta, and end</li><li>reasoning start, delta, and end</li><li>tool start, args, result, and end</li><li>tool background task events</li><li>background operation started, plus terminal or meaningful status events</li></ul><p>Live-only or caveated families:</p><ul><li>audio transcript streaming is not durable by default</li><li>interactive request/response events are live bidirectional flows</li><li>workflow and subagent child-event persistence depends on the execution path and session/branch policy</li><li>observability and diagnostic events generally do not persist unless explicitly mapped</li><li>compaction observability is live, while branch-history compaction is durable only when hard retention is applied</li><li>message-turn errors have special branch failure conversion behavior rather than the same simple persistence override as successful turn events</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("reference/events.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const events = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  events as default
};
