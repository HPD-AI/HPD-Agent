import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Provider Keys And Environment Variables","description":"","frontmatter":{},"headers":[],"relativePath":"reference/provider-keys-and-env-vars.md","filePath":"reference/provider-keys-and-env-vars.md","lastUpdated":null}');
const _sfc_main = { name: "reference/provider-keys-and-env-vars.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="provider-keys-and-environment-variables" tabindex="-1">Provider Keys And Environment Variables <a class="header-anchor" href="#provider-keys-and-environment-variables" aria-label="Permalink to &quot;Provider Keys And Environment Variables&quot;">​</a></h1><p>This page lists provider keys and public environment variable aliases for chat setup.</p><table tabindex="0"><thead><tr><th>Package</th><th>Provider key</th><th>Model means</th><th>Required or common environment variables</th></tr></thead><tbody><tr><td><code>HPD-Agent.Providers.OpenAI</code></td><td><code>openai</code></td><td>OpenAI model id</td><td><code>OPENAI_API_KEY</code>; optional custom endpoint by convention: <code>OPENAI_ENDPOINT</code></td></tr><tr><td><code>HPD-Agent.Providers.OpenAI</code></td><td><code>azure-openai</code></td><td>Azure OpenAI deployment name</td><td><code>AZURE_OPENAI_ENDPOINT</code>, <code>AZURE_OPENAI_API_KEY</code></td></tr><tr><td><code>HPD-Agent.Providers.AzureAI</code></td><td><code>azure-ai</code></td><td>Azure AI Foundry/Projects or Azure OpenAI deployment name</td><td><code>AZURE_AI_ENDPOINT</code>; <code>AZURE_AI_API_KEY</code> only when not using OAuth</td></tr><tr><td><code>HPD-Agent.Providers.Anthropic</code></td><td><code>anthropic</code></td><td>Claude model id</td><td><code>ANTHROPIC_API_KEY</code></td></tr><tr><td><code>HPD-Agent.Providers.GoogleAI</code></td><td><code>google-ai</code></td><td>Gemini model id</td><td>registered aliases: <code>GOOGLE_API_KEY</code> or <code>GEMINI_API_KEY</code>; convention-supported: <code>GOOGLE_AI_API_KEY</code></td></tr><tr><td><code>HPD-Agent.Providers.Ollama</code></td><td><code>ollama</code></td><td>Local Ollama model tag</td><td>Fluent setup can read <code>OLLAMA_ENDPOINT</code> or <code>OLLAMA_HOST</code>; JSON/config should set <code>Endpoint</code> when not using localhost</td></tr><tr><td><code>HPD-Agent.Providers.HuggingFace</code></td><td><code>huggingface</code></td><td>Hugging Face repository id</td><td><code>HUGGINGFACE_API_KEY</code> or <code>HF_TOKEN</code></td></tr><tr><td><code>HPD-Agent.Providers.Mistral</code></td><td><code>mistral</code></td><td>Mistral model id</td><td><code>MISTRAL_API_KEY</code></td></tr><tr><td><code>HPD-Agent.Providers.Bedrock</code></td><td><code>bedrock</code></td><td>Amazon Bedrock model id</td><td><code>AWS_REGION</code> or <code>AWS_DEFAULT_REGION</code>; credentials usually come from the AWS SDK default credential chain</td></tr><tr><td><code>HPD-Agent.Providers.OnnxRuntime</code></td><td><code>onnx-runtime</code></td><td>Local ONNX Runtime GenAI model directory</td><td><code>ONNX_MODEL_PATH</code> for provider-config setup</td></tr><tr><td><code>HPD-Agent.Providers.Audio.OpenAI</code></td><td><code>openai</code></td><td>Audio model id for STT/TTS/realtime families</td><td><code>OPENAI_API_KEY</code>; optional endpoint aliases: <code>OPENAI_ENDPOINT</code>, <code>OPENAI_BASE_URL</code></td></tr><tr><td><code>HPD-Agent.Providers.Audio.ElevenLabs</code></td><td><code>elevenlabs</code></td><td>ElevenLabs STT/TTS model or voice setup</td><td><code>ELEVENLABS_API_KEY</code></td></tr><tr><td><code>HPD-Agent.Providers.OpenRouter</code></td><td><code>openrouter</code></td><td>OpenRouter model id</td><td>No fluent <code>AgentBuilder</code> helper in the current package; <code>OPENROUTER_API_KEY</code></td></tr></tbody></table><h2 id="configuration-keys" tabindex="-1">Configuration Keys <a class="header-anchor" href="#configuration-keys" aria-label="Permalink to &quot;Configuration Keys&quot;">​</a></h2><p>When configuration is available, the default resolver checks provider-scoped keys. Prefer exact provider keys:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>openai:ApiKey</span></span>
<span class="line"><span>Providers:openai:ApiKey</span></span>
<span class="line"><span>azure-openai:Endpoint</span></span>
<span class="line"><span>Providers:azure-openai:Endpoint</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>Explicit values in <code>ClientProviderConfig</code> or fluent setup take precedence over resolver lookup.</p><p>Bedrock credential env variables such as <code>AWS_ACCESS_KEY_ID</code>, <code>AWS_SECRET_ACCESS_KEY</code>, and <code>AWS_SESSION_TOKEN</code> are accepted by the AWS SDK default credential chain and are also registered as <code>bedrock:*</code> aliases. Current client creation does not copy explicit credential fields from <code>ISecretResolver</code> into <code>BedrockProviderConfig</code>; use normal AWS SDK credential configuration or explicit typed config.</p><h2 id="live-smoke-gates" tabindex="-1">Live Smoke Gates <a class="header-anchor" href="#live-smoke-gates" aria-label="Permalink to &quot;Live Smoke Gates&quot;">​</a></h2><p>Live provider smokes are opt-in so ordinary test runs do not spend provider quota or require local model assets.</p><table tabindex="0"><thead><tr><th>Smoke</th><th>Required variables</th></tr></thead><tbody><tr><td>OpenAI text-to-speech</td><td><code>HPD_AUDIO_LIVE_SMOKE=1</code>, <code>OPENAI_API_KEY</code></td></tr><tr><td>OpenAI realtime agent turn</td><td><code>HPD_REALTIME_LIVE_SMOKE=1</code>, <code>OPENAI_API_KEY</code></td></tr><tr><td>ElevenLabs text-to-speech</td><td><code>HPD_AUDIO_LIVE_SMOKE=1</code>, <code>ELEVENLABS_API_KEY</code></td></tr><tr><td>ElevenLabs realtime speech-to-text</td><td><code>ELEVENLABS_API_KEY</code> and a caller/test that sends PCM audio through the STT streaming path</td></tr><tr><td>ONNX Runtime local inference</td><td><code>ONNX_MODEL_PATH</code></td></tr></tbody></table><h2 id="azure-terms" tabindex="-1">Azure Terms <a class="header-anchor" href="#azure-terms" aria-label="Permalink to &quot;Azure Terms&quot;">​</a></h2><p>Use precise Azure wording:</p><ul><li><code>azure-openai</code> is the traditional Azure OpenAI resource path. Its <code>ModelName</code> is a deployment name, and setup requires an endpoint and API key.</li><li><code>azure-ai</code> is the current Azure AI Projects / Foundry path. It also supports traditional Azure OpenAI endpoints. Foundry/Projects endpoints use <code>services.ai.azure.com</code> with <code>/api/projects/</code> and require OAuth; passing an API key for that endpoint shape is invalid.</li></ul><h2 id="setup-caveat" tabindex="-1">Setup Caveat <a class="header-anchor" href="#setup-caveat" aria-label="Permalink to &quot;Setup Caveat&quot;">​</a></h2><p>Some providers validate <code>ApiKey</code>, <code>Endpoint</code>, or <code>Region</code> before deferred secret resolution. Keep provider setup examples explicit when you need self-contained configuration, and use environment aliases when deployment configuration owns the secret values.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("reference/provider-keys-and-env-vars.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const providerKeysAndEnvVars = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  providerKeysAndEnvVars as default
};
